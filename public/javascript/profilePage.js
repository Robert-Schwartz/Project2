const multer = require('multer');

var formSubmit = function(event) {
    event.preventDefault();

    /*Define querey selectors*/

    let username = document.querySelector();

    const submit = await fetch(`/upload`, {
        method: 'POST',
        body: multer
    })
}