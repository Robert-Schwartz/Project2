<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyGame</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
    <script src="https://kit.fontawesome.com/c55b01837c.js" crossorigin="anonymous">
    </script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <nav class="navbar is-dark">
        <div class="is-half">
            <i class="fas fa-user-astronaut"></i>
            <a href="/" class="button is-primary">Home</a>
            {{#if loggedIn}}
            <a href="/profile">Profile</a>
            <button class="button is-danger is-medium home-logout" id="logout">Logout</button>
            {{else}}
            <a href="/login" class="button is-success">login</a>
            {{/if}}
        </div>

    </nav>
    {{#if loggedIn}}
    <a href="/createStats">
        <button id="new" class="button is-link is-light is-medium is-fullwidth">Add Stats</button>
    </a>
    {{/if}}


    <section class="section">
    {{> hero}}
    </section>

    <main>
        {{{ body }}}
    </main>
    <footer>
    {{>footer}}

    </footer>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>